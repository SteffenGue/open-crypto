name: cryptocompare
exchange: false
api_key:

rate_limit:
  max:
  unit:

api_url: https://min-api.cryptocompare.com/data/

requests:
  currency_pairs:
    request:
      template: blockchain/list
      pair_template: null
      params: null

    response:
      type:

    mapping:
      - key: currency_pair_first
        path:
        type:
      - key: currency_pair_second
        path:
        type:

  blockchain:
